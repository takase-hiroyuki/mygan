以下の SQL 文を実行したときに、

SELECT 
    p.proname AS function_name,
    pg_get_functiondef(p.oid) AS function_definition
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public'
ORDER BY p.proname;

結果は、以下の JSON となった

[
  {
    "function_name": "action_check_calculations",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_check_calculations(p_room_id text, p_user_id text, p_input_income integer, p_input_cashflow integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_flags JSONB;\n    v_correct_income INT;\n    v_correct_cashflow INT;\nBEGIN\n    RAISE NOTICE '[DEBUG] action_check_calculations START: user_id=%, input_income=%, input_cashflow=%', p_user_id, p_input_income, p_input_cashflow;\n\n    SELECT * INTO v_participant\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RAISE NOTICE '[DEBUG] action_check_calculations ERROR: プレイヤーが見つかりません';\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、計算チェックの必要はありません。');\n    END IF;\n\n    v_correct_income := COALESCE((v_state->'financials'->>'total_income')::INT, 0);\n    v_correct_cashflow := COALESCE((v_state->'financials'->>'net_cash_flow')::INT, 0);\n\n    IF p_input_income != v_correct_income OR p_input_cashflow != v_correct_cashflow THEN\n        RAISE NOTICE '[DEBUG] action_check_calculations FAILED: correct_income=%, correct_cashflow=%', v_correct_income, v_correct_cashflow;\n        RETURN jsonb_build_object(\n            'status', 'error', \n            'message', '計算結果が正しくありません。もう一度計算し直してください。'\n        );\n    END IF;\n\n    -- is_calculatingのみを解除し、整数値フラグは保持する\n    v_flags := jsonb_set(v_flags, '{is_calculating}', 'false'::jsonb);\n    v_state := jsonb_set(v_state, '{flags}', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_check_calculations SUCCESS: user_id=%', p_user_id;\n    RETURN jsonb_build_object('status', 'success', 'message', '計算チェック成功！財務諸表の計算が確認されました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "action_land_on_baby",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_land_on_baby(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_financials JSONB;\n    v_expenses JSONB;\n    v_flags JSONB;\n    v_children_count INT;\n    v_per_child_expense INT;\n    v_current_child_expense INT;\n    v_total_expenses INT;\n    v_net_cash_flow INT;\n    v_total_income INT;\nBEGIN\n    RAISE NOTICE '[DEBUG] action_land_on_baby START: user_id=%', p_user_id;\n\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    IF NOT FOUND THEN\n        RAISE EXCEPTION 'Player not found';\n    END IF;\n\n    v_state := v_participant.state;\n    v_financials := v_state->'financials';\n    v_expenses := v_financials->'expenses';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    v_children_count := COALESCE((v_state->>'children_count')::INT, 0);\n    v_per_child_expense := COALESCE((v_financials->>'per_child_expense')::INT, 0);\n\n    IF v_children_count >= 3 THEN\n        RAISE NOTICE '[DEBUG] action_land_on_baby: 子供の数最大 (3)';\n        RETURN jsonb_build_object('status', 'success', 'message', '子供の数はすでに最大（3人）です。費用は変動しません。');\n    END IF;\n\n    v_children_count := v_children_count + 1;\n    v_state := jsonb_set(v_state, '{children_count}', to_jsonb(v_children_count));\n\n    v_current_child_expense := v_children_count * v_per_child_expense;\n    v_expenses := jsonb_set(v_expenses, '{child_expense}', to_jsonb(v_current_child_expense));\n    v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n\n    v_total_expenses := COALESCE((v_financials->>'total_expenses')::INT, 0) + v_per_child_expense;\n    v_financials := jsonb_set(v_financials, '{total_expenses}', to_jsonb(v_total_expenses));\n\n    v_total_income := COALESCE((v_financials->>'total_income')::INT, 0);\n    v_net_cash_flow := v_total_income - v_total_expenses;\n    v_financials := jsonb_set(v_financials, '{net_cash_flow}', to_jsonb(v_net_cash_flow));\n\n    v_state := jsonb_set(v_state, '{financials}', v_financials);\n    v_flags := jsonb_set(v_flags, '{is_calculating}', 'true'::jsonb);\n    v_state := jsonb_set(v_state, '{flags}', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_land_on_baby END: user_id=%, children_count=%', p_user_id, v_children_count;\n    RETURN jsonb_build_object('status', 'success', 'message', '子供が生まれました。財務諸表を計算してください。');\nEND;\n$function$\n"
  },
  {
    "function_name": "claim_paycheck",
    "function_definition": "CREATE OR REPLACE FUNCTION public.claim_paycheck(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_flags jsonb;\n    v_pending_paydays int;\n    v_net_cash_flow int;\n    v_total_payout int;\nBEGIN\n    RAISE NOTICE '[DEBUG] claim_paycheck START: user_id=%', p_user_id;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN \n        RAISE EXCEPTION 'Invalid user or room.'; \n    END IF;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_pending_paydays := COALESCE((v_flags->>'pending_paydays')::int, 0);\n\n    RAISE NOTICE '[DEBUG] claim_paycheck: pending_paydays=%', v_pending_paydays;\n\n    IF v_pending_paydays <= 0 THEN\n        RAISE EXCEPTION '現在、給料受け取りの権利はありません。';\n    END IF;\n\n    v_net_cash_flow := COALESCE((v_state->'financials'->>'net_cash_flow')::int, 0);\n    v_total_payout := v_net_cash_flow * v_pending_paydays;\n\n    -- フラグ状態の取得と更新。受け取ったので pending_paydays を 0 にリセット\n    v_flags := v_flags\n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('pending_paydays', 0);\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object(\n        'financials', v_state->'financials' || jsonb_build_object(\n            'cash', COALESCE((v_state->'financials'->>'cash')::int, 0) + v_total_payout\n        ),\n        'calculation_phase', 'none',\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] claim_paycheck END: total_payout=%', v_total_payout;\nEND;\n$function$\n"
  },
  {
    "function_name": "draw_card",
    "function_definition": "CREATE OR REPLACE FUNCTION public.draw_card(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_state jsonb;\n    v_flags jsonb;\nBEGIN\n    RAISE NOTICE '[DEBUG] draw_card START: user_id=%', p_user_id;\n\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms\n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    SELECT state INTO v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF v_state IS NULL THEN\n        RAISE EXCEPTION '指定されたプレイヤーが存在しません。';\n    END IF;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'サイコロを振る前にカードを引くことはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RAISE EXCEPTION 'すでにカードを引いています。重複して引くことはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '計算フェーズ中（給料処理など）のため、カードを引くことはできません。';\n    END IF;\n\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object('flags', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] draw_card END: is_card_drawn set to true';\nEND;\n$function$\n"
  },
  {
    "function_name": "merge_participant_state",
    "function_definition": "CREATE OR REPLACE FUNCTION public.merge_participant_state(target_user_id text, state_patch jsonb)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    RAISE NOTICE '[DEBUG] merge_participant_state: target_user_id=%', target_user_id;\n    UPDATE participants \n    SET state = COALESCE(state, '{}'::jsonb) || state_patch \n    WHERE user_id = target_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "pass_and_end_turn",
    "function_definition": "CREATE OR REPLACE FUNCTION public.pass_and_end_turn(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_next_id text;\n    v_state jsonb;\n    v_flags jsonb;\n    v_net_cash_flow int;\nBEGIN\n    RAISE NOTICE '[DEBUG] pass_and_end_turn START: user_id=%', p_user_id;\n\n    SELECT current_turn_user_id INTO v_current_turn_user_id \n    FROM rooms \n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'サイコロを振る前にターンを終了することはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '計算フェーズ中のため、ターンを終了することはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_negative_cash_flow')::boolean, false) THEN\n        RAISE EXCEPTION 'マイナスキャッシュフローの未処理問題があるため、ターンを終了することはできません。';\n    END IF;\n\n    SELECT user_id INTO v_next_id \n    FROM participants\n    WHERE room_id = p_room_id \n      AND id > (SELECT id FROM participants WHERE room_id = p_room_id AND user_id = p_user_id)\n    ORDER BY id ASC LIMIT 1;\n\n    IF v_next_id IS NULL THEN\n        SELECT user_id INTO v_next_id \n        FROM participants \n        WHERE room_id = p_room_id \n        ORDER BY id ASC LIMIT 1;\n    END IF;\n\n    v_net_cash_flow := COALESCE((v_state->'financials'->>'net_cash_flow')::int, 0);\n\n    -- ★修正: pending_paydays を 0 にリセットし、受け取り忘れの給料の権利を消滅させる\n    v_flags := jsonb_build_object(\n        'has_rolled_dice', false,\n        'is_card_drawn', false,\n        'is_action_completed', false,\n        'is_calculating', false,\n        'is_negative_cash_flow', (v_net_cash_flow < 0),\n        'charity_turns_left', COALESCE((v_flags->>'charity_turns_left')::int, 0),\n        'downsized_turns_left', COALESCE((v_flags->>'downsized_turns_left')::int, 0),\n        'pending_paydays', 0 \n    );\n\n    UPDATE rooms \n    SET current_turn_user_id = v_next_id \n    WHERE id = p_room_id;\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object(\n        'last_dice', 0,\n        'calculation_phase', 'none',\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] pass_and_end_turn END: next_user_id=%', v_next_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "roll_dice_and_move",
    "function_definition": "CREATE OR REPLACE FUNCTION public.roll_dice_and_move(p_room_id text, p_user_id text, p_dice_count integer DEFAULT 1)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_flags jsonb;\n    v_dice int := 0;\n    v_pos int;\n    v_payday_count int := 0;\n    v_charity_turns_left int;\n    v_pending_paydays int;\nBEGIN\n    RAISE NOTICE '[DEBUG] roll_dice_and_move START: user_id=%, dice_count=%', p_user_id, p_dice_count;\n\n    SELECT state INTO v_state FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    \n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_charity_turns_left := COALESCE((v_flags->>'charity_turns_left')::int, 0);\n    v_pending_paydays := COALESCE((v_flags->>'pending_paydays')::int, 0);\n\n    IF COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'すでにサイコロを振っています。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '現在計算フェーズ中（または未処理のイベントあり）のため、サイコロを振れません。';\n    END IF;\n\n    -- 引数ガードと寄付権利チェック\n    IF p_dice_count < 1 OR p_dice_count > 2 THEN\n        RAISE EXCEPTION 'サイコロの数は1または2のみ指定可能です。';\n    END IF;\n\n    IF p_dice_count = 2 AND v_charity_turns_left <= 0 THEN\n        RAISE EXCEPTION '寄付の権利がないため、サイコロを2個振ることはできません。';\n    END IF;\n\n    -- サイコロ計算\n    FOR i IN 1..p_dice_count LOOP\n        v_dice := v_dice + (floor(random() * 6) + 1)::int;\n    END LOOP;\n    v_pos := COALESCE((v_state->>'position')::int, 0);\n\n    -- ペイデイ複数回通過判定\n    FOR i IN 1..v_dice LOOP\n        IF ((v_pos + i) % 24) IN (0, 5, 11, 18) THEN \n            v_payday_count := v_payday_count + 1; \n        END IF;\n    END LOOP;\n\n    -- 寄付ターンの消費\n    IF v_charity_turns_left > 0 THEN\n        v_charity_turns_left := v_charity_turns_left - 1;\n    END IF;\n    \n    v_pending_paydays := v_pending_paydays + v_payday_count;\n\n    v_flags := v_flags \n        || jsonb_build_object('has_rolled_dice', true)\n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('charity_turns_left', v_charity_turns_left)\n        || jsonb_build_object('pending_paydays', v_pending_paydays);\n\n    UPDATE participants \n    SET state = v_state || jsonb_build_object(\n        'position', (v_pos + v_dice) % 24,\n        'last_dice', v_dice,\n        'calculation_phase', CASE WHEN v_pending_paydays > 0 THEN 'payday' ELSE 'none' END,\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] roll_dice_and_move END: new_pos=%, payday_count=%, remaining_charity=%', ((v_pos + v_dice) % 24), v_payday_count, v_charity_turns_left;\nEND;\n$function$\n"
  },
  {
    "function_name": "start_game_with_professions",
    "function_definition": "CREATE OR REPLACE FUNCTION public.start_game_with_professions(p_room_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    r_part RECORD;\n    r_prof RECORD;\n    cur_prof CURSOR FOR SELECT id, name, starting_cash::varchar::int AS starting_cash, salary::varchar::int AS salary, expenses, initial_liabilities FROM professions ORDER BY RANDOM();\n    v_total_exp int;\n    v_net_cash_flow int;\n    v_per_child_expense int;\n    \n    v_deck_small jsonb;\n    v_deck_big jsonb;\n    v_deck_market jsonb;\n    v_deck_doodad jsonb;\nBEGIN\n    RAISE NOTICE '[DEBUG] start_game_with_professions START: room_id=%', p_room_id;\n\n    PERFORM id FROM rooms WHERE id = p_room_id FOR UPDATE;\n    PERFORM user_id FROM participants WHERE room_id = p_room_id FOR UPDATE;\n\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_small FROM cards WHERE deck_type = 'small_deal';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_big FROM cards WHERE deck_type = 'big_deal';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_market FROM cards WHERE deck_type = 'market';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_doodad FROM cards WHERE deck_type = 'doodad';\n\n    OPEN cur_prof;\n\n    FOR r_part IN SELECT user_id, state FROM participants WHERE room_id = p_room_id LOOP\n        FETCH cur_prof INTO r_prof;\n        \n        IF NOT FOUND THEN\n            CLOSE cur_prof;\n            OPEN cur_prof;\n            FETCH cur_prof INTO r_prof;\n        END IF;\n\n        v_per_child_expense := COALESCE(\n            (r_prof.expenses->>'per_child_expense')::int,\n            (r_prof.expenses->>'child_expense')::int, \n            0\n        );\n\n        v_total_exp := \n            COALESCE((r_prof.expenses->>'taxes')::int, 0) + \n            COALESCE((r_prof.expenses->>'other_expenses')::int, 0) + \n            COALESCE((r_prof.expenses->>'retail_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'car_loan_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'mortgage_payment')::int, 0);\n\n        v_net_cash_flow := r_prof.salary - v_total_exp;\n\n        UPDATE participants \n        SET state = jsonb_build_object(\n            'name', COALESCE(r_part.state->>'name', 'Player'),\n            'role', COALESCE(r_part.state->>'role', 'general'),\n            'game_phase', 'rat_race',\n            'position', 0, \n            'last_dice', 0, \n            'calculation_phase', 'none', \n            'profession', r_prof.name, \n            'profession_id', r_prof.id,\n            'children_count', 0, \n            'flags', jsonb_build_object(\n                'has_rolled_dice', false,\n                'is_card_drawn', false,\n                'is_action_completed', false,\n                'is_calculating', false,\n                'is_negative_cash_flow', (v_net_cash_flow < 0),\n                'charity_turns_left', 0,\n                'downsized_turns_left', 0,\n                'pending_paydays', 0\n            ),\n            'financials', jsonb_build_object(\n                'cash', r_prof.starting_cash, \n                'salary', r_prof.salary,\n                'total_income', r_prof.salary,\n                'passive_income', 0,\n                'total_expenses', v_total_exp,\n                'net_cash_flow', v_net_cash_flow,\n                'per_child_expense', v_per_child_expense,\n                'expenses', COALESCE(r_prof.expenses, '{}'::jsonb) || jsonb_build_object(\n                    'bank_loan_payment', 0,\n                    'child_expense', 0\n                ),\n                'liabilities', COALESCE(r_prof.initial_liabilities, '{}'::jsonb) || jsonb_build_object(\n                    'bank_loan', 0\n                ), \n                'assets', jsonb_build_object('stocks', '{}'::jsonb, 'real_estate', '[]'::jsonb)\n            )\n        )\n        WHERE room_id = p_room_id AND user_id = r_part.user_id;\n    END LOOP;\n    \n    CLOSE cur_prof;\n\n    UPDATE rooms SET \n        game_state = jsonb_build_object(\n            'status', 'playing', \n            'decks', jsonb_build_object(\n                'small_deal', v_deck_small, \n                'big_deal', v_deck_big, \n                'market', v_deck_market, \n                'doodad', v_deck_doodad\n            ), \n            'current_card', null\n        ),\n        current_turn_user_id = (SELECT user_id FROM participants WHERE room_id = p_room_id ORDER BY RANDOM() LIMIT 1)\n    WHERE id = p_room_id;\n\n    RAISE NOTICE '[DEBUG] start_game_with_professions END: Initialize completes';\nEND;\n$function$\n"
  }
]
