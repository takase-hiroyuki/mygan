以下の SQL 文を実行したときに、

SELECT 
    p.proname AS function_name,
    pg_get_functiondef(p.oid) AS function_definition
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public'
ORDER BY p.proname;

結果は、以下の JSON となった

[
  {
    "function_name": "action_bankrupt_and_remove",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_bankrupt_and_remove(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    RAISE NOTICE '[DEBUG] action_bankrupt_and_remove START: user_id=%', p_user_id;\n\n    -- 1. まず手番を次のプレイヤーに回す（既存の関数を内部で呼び出し）\n    PERFORM public.pass_and_end_turn(p_room_id, p_user_id);\n\n    -- 2. 破産したプレイヤーのデータをデータベースから完全に削除する\n    DELETE FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_bankrupt_and_remove END: user_id=%', p_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "action_bankrupt_and_remove_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_bankrupt_and_remove_v2(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_user_name text;\nBEGIN\n    -- ログ記録用にユーザー名を取得\n    SELECT COALESCE(state->>'name', 'Unknown') INTO v_user_name\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 1. まず手番を次のプレイヤーに回す（_v2版を呼び出し）\n    PERFORM public.pass_and_end_turn_v2(p_room_id, p_user_id);\n\n    -- 2. 破産したプレイヤーのデータをデータベースから完全に削除する\n    DELETE FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 3. ログの記録\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, '破産',\n        'プレイヤーが破産し、ゲームから退場しました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "action_check_calculations",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_check_calculations(p_room_id text, p_user_id text, p_input_income integer, p_input_cashflow integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_flags JSONB;\n    v_correct_income INT;\n    v_correct_cashflow INT;\nBEGIN\n    RAISE NOTICE '[DEBUG] action_check_calculations START: user_id=%, input_income=%, input_cashflow=%', p_user_id, p_input_income, p_input_cashflow;\n\n    SELECT * INTO v_participant\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、計算チェックの必要はありません。');\n    END IF;\n\n    v_correct_income := COALESCE((v_state->'financials'->>'total_income')::INT, 0);\n    v_correct_cashflow := COALESCE((v_state->'financials'->>'net_cash_flow')::INT, 0);\n\n    IF p_input_income != v_correct_income OR p_input_cashflow != v_correct_cashflow THEN\n        RETURN jsonb_build_object(\n            'status', 'error', \n            'message', '計算結果が正しくありません。もう一度計算し直してください。'\n        );\n    END IF;\n\n    -- ★修正: 計算解除と同時に、マイナスキャッシュフローのフラグを正確に再評価して更新する\n    v_flags := v_flags \n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('is_negative_cash_flow', (v_correct_cashflow < 0));\n\n    v_state := jsonb_set(v_state, '{flags}', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_check_calculations SUCCESS: user_id=%', p_user_id;\n    RETURN jsonb_build_object('status', 'success', 'message', '計算チェック成功！財務諸表の計算が確認されました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "action_check_calculations_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_check_calculations_v2(p_room_id text, p_user_id text, p_input_income integer, p_input_cashflow integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_flags JSONB;\n    v_correct_income INT;\n    v_correct_cashflow INT;\n    v_user_name text; -- ログ用のユーザー名変数\nBEGIN\n    SELECT * INTO v_participant\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    \n    -- ★ 追加: ユーザー名の取得\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    \n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、計算チェックの必要はありません。');\n    END IF;\n\n    v_correct_income := COALESCE((v_state->'financials'->>'total_income')::INT, 0);\n    v_correct_cashflow := COALESCE((v_state->'financials'->>'net_cash_flow')::INT, 0);\n\n    IF p_input_income != v_correct_income OR p_input_cashflow != v_correct_cashflow THEN\n        RETURN jsonb_build_object(\n            'status', 'error', \n            'message', '計算結果が正しくありません。もう一度計算し直してください。'\n        );\n    END IF;\n\n    v_flags := v_flags \n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('is_negative_cash_flow', (v_correct_cashflow < 0));\n\n    v_state := jsonb_set(v_state, '{flags}', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ★ 追加: game_logs テーブルへの書き込み\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        '財務監査',\n        '財務諸表の計算チェックに合格しました。'\n    );\n\n    RETURN jsonb_build_object('status', 'success', 'message', '計算チェック成功！財務諸表の計算が確認されました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "action_donate_charity",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_donate_charity(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_financials jsonb;\n    v_flags jsonb;\n    v_total_income int;\n    v_donation_amount int;\n    v_cash int;\nBEGIN\n    RAISE NOTICE '[DEBUG] action_donate_charity START: user_id=%', p_user_id;\n\n    -- プレイヤー状態の取得（行ロック）\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_financials := v_state->'financials';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    -- 総収入の取得と寄付額（10%）の計算\n    v_total_income := COALESCE((v_financials->>'total_income')::int, 0);\n    v_donation_amount := v_total_income / 10;\n    \n    v_cash := COALESCE((v_financials->>'cash')::int, 0);\n\n    -- 現金から寄付額を差し引く\n    v_cash := v_cash - v_donation_amount;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n\n    -- 寄付フラグ（向こう3ターン）を設定\n    v_flags := jsonb_set(v_flags, '{charity_turns_left}', '3'::jsonb);\n\n    -- 状態の保存\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    UPDATE participants SET state = v_state WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_donate_charity END: user_id=%', p_user_id;\n    RETURN jsonb_build_object(\n        'status', 'success', \n        'message', '総収入の10%（$' || v_donation_amount || '）を寄付しました。向こう3ターンの間、サイコロを2個振ることができます！'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "action_donate_charity_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_donate_charity_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_financials JSONB;\n    v_flags JSONB;\n    v_total_income INT;\n    v_cash INT;\n    v_cost INT;\n    v_user_name text;\nBEGIN\n    SELECT * INTO v_participant\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    v_financials := COALESCE(v_state->'financials', '{}'::jsonb);\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF COALESCE((v_flags->>'is_action_completed')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '既にアクションを完了しています。');\n    END IF;\n\n    v_total_income := COALESCE((v_financials->>'total_income')::INT, 0);\n    v_cash := COALESCE((v_financials->>'cash')::INT, 0);\n    v_cost := FLOOR(v_total_income * 0.10);\n\n    -- 現金不足チェック\n    IF v_cash < v_cost THEN\n        RETURN jsonb_build_object(\n            'status', 'error', \n            'message', '現金が足りません。銀行ローンを借りて、現金を増やしなさい。'\n        );\n    END IF;\n\n    -- 支払いとフラグ更新\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash - v_cost));\n    v_flags := v_flags || jsonb_build_object(\n        'charity_turns_left', 3,\n        'is_action_completed', true\n    );\n    v_state := v_state || jsonb_build_object('financials', v_financials, 'flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, '寄付', \n        '総収入の10%（$' || v_cost || '）を寄付しました。向こう3ターンの間、サイコロを2個振ることができます。'\n    );\n\n    RETURN jsonb_build_object('status', 'success', 'message', '寄付が完了しました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "action_draw_big_deal_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_draw_big_deal_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_decks JSONB;\n    v_deck JSONB;\n    v_card_id INT;\n    v_card JSONB;\n    v_flags JSONB;\n    v_user_name text;\nBEGIN\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    v_flags := COALESCE(v_participant.state->'flags', '{}'::jsonb);\n    v_user_name := COALESCE(v_participant.state->>'name', 'Unknown');\n\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'すでにカードを引いています。');\n    END IF;\n\n    v_decks := v_room.game_state->'decks';\n    v_deck := v_decks->'big_deal';\n\n    IF jsonb_array_length(v_deck) = 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'Big Dealの山札が空です。ホストにシャッフルを依頼してください。');\n    END IF;\n\n    v_card_id := (v_deck->>0)::int;\n    IF v_card_id IS NULL THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '山札データの破損：カードIDが取得できませんでした。');\n    END IF;\n\n    v_deck := v_deck - 0;\n    v_decks := jsonb_set(v_decks, '{big_deal}', v_deck);\n\n    -- 🌟修正箇所\n    SELECT to_jsonb(c.*) INTO v_card FROM cards c WHERE c.id = v_card_id;\n    IF v_card IS NULL THEN\n         RETURN jsonb_build_object('status', 'error', 'message', 'カードがデータベースに見つかりませんでした。');\n    END IF;\n\n    UPDATE rooms\n    SET game_state = jsonb_set(\n           jsonb_set(v_room.game_state, '{decks}', v_decks),\n           '{current_card}', v_card || jsonb_build_object(\n               'status', 'active',\n               'owner_user_id', p_user_id\n           )\n        )\n    WHERE id = p_room_id;\n\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n    UPDATE participants\n    SET state = jsonb_set(v_participant.state, '{flags}', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, 'カードドロー',\n        'Big Dealカード「' || COALESCE(v_card->>'title', '不明') || '」を引きました。'\n    );\n\n    RETURN v_card;\nEND;\n$function$\n"
  },
  {
    "function_name": "action_draw_doodad",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_draw_doodad(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_decks JSONB;\n    v_doodad_deck JSONB;\n    v_card_id INT;\n    v_card RECORD;\n    v_flags JSONB;\nBEGIN\n    -- 1. 部屋情報の取得と手番確認\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    -- 2. プレイヤー情報の取得\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    v_flags := COALESCE(v_participant.state->'flags', '{}'::jsonb);\n\n    -- 既に引いているかのガード\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'すでにカードを引いています。');\n    END IF;\n\n    -- 3. 山札（Doodad）からカードIDを取得\n    v_decks := v_room.game_state->'decks';\n    v_doodad_deck := v_decks->'doodad';\n\n    IF jsonb_array_length(v_doodad_deck) = 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '無駄遣い（Doodad）の山札が空です。ホストにシャッフルを依頼してください。');\n    END IF;\n\n    v_card_id := (v_doodad_deck->>0)::int;\n    \n    IF v_card_id IS NULL THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '山札データの破損：カードIDが取得できませんでした。');\n    END IF;\n\n    v_doodad_deck := v_doodad_deck - 0;\n    v_decks := jsonb_set(v_decks, '{doodad}', v_doodad_deck);\n\n    -- 4. カードの詳細データを取得\n    SELECT * INTO v_card FROM cards WHERE id = v_card_id;\n\n    IF v_card.id IS NULL THEN\n         RETURN jsonb_build_object('status', 'error', 'message', 'ID: ' || v_card_id || ' のカードがデータベースに見つかりませんでした。');\n    END IF;\n\n    -- 5. 部屋の状態（山札の減少と現在のカード）をスキーマに合わせて更新\n    UPDATE rooms\n    SET game_state = jsonb_set(\n           jsonb_set(v_room.game_state, '{decks}', v_decks),\n           '{current_card}', jsonb_build_object(\n               'status', 'active',\n               'owner_user_id', p_user_id,\n               'card_id', v_card.id,\n               'deck_type', v_card.deck_type,\n               'title', v_card.title,\n               'description', v_card.description,\n               'cost', v_card.cost,\n               'action_rule', v_card.action_rule\n           )\n        )\n    WHERE id = p_room_id;\n\n    -- 6. プレイヤーのフラグ（カードを引いた状態）を更新\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n    UPDATE participants\n    SET state = jsonb_set(v_participant.state, '{flags}', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 7. フロントエンドにカードデータをそのまま返す\n    RETURN to_jsonb(v_card);\nEND;\n$function$\n"
  },
  {
    "function_name": "action_draw_doodad_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_draw_doodad_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_decks JSONB;\n    v_doodad_deck JSONB;\n    v_card_id INT;\n    v_card RECORD;\n    v_flags JSONB;\n    v_user_name text; -- ログ表示用のユーザー名\nBEGIN\n    -- 1. 部屋情報の取得と手番確認\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    -- 2. プレイヤー情報の取得\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    v_flags := COALESCE(v_participant.state->'flags', '{}'::jsonb);\n    \n    -- ★ 追加: プレイヤー名を取得\n    v_user_name := COALESCE(v_participant.state->>'name', 'Unknown');\n\n    -- 既に引いているかのガード\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'すでにカードを引いています。');\n    END IF;\n\n    -- 3. 山札（Doodad）からカードIDを取得\n    v_decks := v_room.game_state->'decks';\n    v_doodad_deck := v_decks->'doodad';\n\n    IF jsonb_array_length(v_doodad_deck) = 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '無駄遣い（Doodad）の山札が空です。ホストにシャッフルを依頼してください。');\n    END IF;\n\n    v_card_id := (v_doodad_deck->>0)::int;\n    \n    IF v_card_id IS NULL THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '山札データの破損：カードIDが取得できませんでした。');\n    END IF;\n\n    v_doodad_deck := v_doodad_deck - 0;\n    v_decks := jsonb_set(v_decks, '{doodad}', v_doodad_deck);\n\n    -- 4. カードの詳細データを取得\n    SELECT * INTO v_card FROM cards WHERE id = v_card_id;\n\n    IF v_card.id IS NULL THEN\n         RETURN jsonb_build_object('status', 'error', 'message', 'ID: ' || v_card_id || ' のカードがデータベースに見つかりませんでした。');\n    END IF;\n\n    -- 5. 部屋の状態（山札の減少と現在のカード）をスキーマに合わせて更新\n    UPDATE rooms\n    SET game_state = jsonb_set(\n           jsonb_set(v_room.game_state, '{decks}', v_decks),\n           '{current_card}', jsonb_build_object(\n               'status', 'active',\n               'owner_user_id', p_user_id,\n               'card_id', v_card.id,\n               'deck_type', v_card.deck_type,\n               'title', v_card.title,\n               'description', v_card.description,\n               'cost', v_card.cost,\n               'action_rule', v_card.action_rule\n           )\n        )\n    WHERE id = p_room_id;\n\n    -- 6. プレイヤーのフラグ（カードを引いた状態）を更新\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n    UPDATE participants\n    SET state = jsonb_set(v_participant.state, '{flags}', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ★ 7. 追加: game_logs テーブルへカードドロー結果をINSERT\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        'カードドロー',\n        'Doodadカード「' || v_card.title || '」を引きました。 - ' || v_card.description || ' (費用: $' || COALESCE(v_card.cost, 0) || ')'\n    );\n\n    -- 8. フロントエンドにカードデータをそのまま返す\n    RETURN to_jsonb(v_card);\nEND;\n$function$\n"
  },
  {
    "function_name": "action_draw_market_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_draw_market_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_decks JSONB;\n    v_deck JSONB;\n    v_card_id INT;\n    v_card JSONB;\n    v_flags JSONB;\n    v_user_name text;\nBEGIN\n    -- 1. 部屋情報の取得と手番確認\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    -- 2. プレイヤー情報の取得\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    v_flags := COALESCE(v_participant.state->'flags', '{}'::jsonb);\n    v_user_name := COALESCE(v_participant.state->>'name', 'Unknown');\n\n    -- 既に引いているかのガード\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'すでにカードを引いています。');\n    END IF;\n\n    -- 3. 山札（Market）からカードIDを取得\n    v_decks := v_room.game_state->'decks';\n    v_deck := v_decks->'market';\n\n    IF jsonb_array_length(v_deck) = 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'Marketの山札が空です。ホストにシャッフルを依頼してください。');\n    END IF;\n\n    v_card_id := (v_deck->>0)::int;\n    IF v_card_id IS NULL THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '山札データの破損：カードIDが取得できませんでした。');\n    END IF;\n\n    -- 先頭のカードを削除して山札を更新\n    v_deck := v_deck - 0;\n    v_decks := jsonb_set(v_decks, '{market}', v_deck);\n\n    -- 4. カードの詳細データを取得し JSONB 化\n    SELECT to_jsonb(c.*) INTO v_card FROM cards c WHERE c.id = v_card_id;\n    IF v_card IS NULL THEN\n         RETURN jsonb_build_object('status', 'error', 'message', 'カードがデータベースに見つかりませんでした。');\n    END IF;\n\n    -- 5. 部屋の状態（山札の減少と現在のカード）を更新\n    UPDATE rooms\n    SET game_state = jsonb_set(\n            jsonb_set(v_room.game_state, '{decks}', v_decks),\n            '{current_card}', v_card || jsonb_build_object(\n                'status', 'active',\n                'owner_user_id', p_user_id\n            )\n        )\n    WHERE id = p_room_id;\n\n    -- 6. プレイヤーのフラグ（カードを引いた状態）を更新\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n    UPDATE participants\n    SET state = jsonb_set(v_participant.state, '{flags}', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 7. ログの書き込み\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, 'Marketカード',\n        'Marketカード「' || COALESCE(v_card->>'title', '不明') || '」を引きました。 - ' || COALESCE((v_card->>'description'), '')\n    );\n\n    RETURN v_card;\nEND;\n$function$\n"
  },
  {
    "function_name": "action_draw_small_deal_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_draw_small_deal_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_decks JSONB;\n    v_deck JSONB;\n    v_card_id INT;\n    v_card JSONB;\n    v_flags JSONB;\n    v_user_name text;\nBEGIN\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    v_flags := COALESCE(v_participant.state->'flags', '{}'::jsonb);\n    v_user_name := COALESCE(v_participant.state->>'name', 'Unknown');\n\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'すでにカードを引いています。');\n    END IF;\n\n    v_decks := v_room.game_state->'decks';\n    v_deck := v_decks->'small_deal';\n\n    IF jsonb_array_length(v_deck) = 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'Small Dealの山札が空です。ホストにシャッフルを依頼してください。');\n    END IF;\n\n    v_card_id := (v_deck->>0)::int;\n    IF v_card_id IS NULL THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '山札データの破損：カードIDが取得できませんでした。');\n    END IF;\n\n    v_deck := v_deck - 0;\n    v_decks := jsonb_set(v_decks, '{small_deal}', v_deck);\n\n    -- 🌟修正箇所: スキーマ不整合エラーを回避するため、行全体をJSONB化\n    SELECT to_jsonb(c.*) INTO v_card FROM cards c WHERE c.id = v_card_id;\n    IF v_card IS NULL THEN\n         RETURN jsonb_build_object('status', 'error', 'message', 'カードがデータベースに見つかりませんでした。');\n    END IF;\n\n    UPDATE rooms\n    SET game_state = jsonb_set(\n           jsonb_set(v_room.game_state, '{decks}', v_decks),\n           '{current_card}', v_card || jsonb_build_object(\n               'status', 'active',\n               'owner_user_id', p_user_id\n           )\n        )\n    WHERE id = p_room_id;\n\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n    UPDATE participants\n    SET state = jsonb_set(v_participant.state, '{flags}', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, 'カードドロー',\n        'Small Dealカード「' || COALESCE(v_card->>'title', '不明') || '」を引きました。'\n    );\n\n    RETURN v_card;\nEND;\n$function$\n"
  },
  {
    "function_name": "action_land_on_downsized",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_land_on_downsized(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_financials JSONB;\n    v_flags JSONB;\n    v_total_expenses INT;\n    v_cash INT;\nBEGIN\n    RAISE NOTICE '[DEBUG] action_land_on_downsized START: user_id=%', p_user_id;\n\n    -- プレイヤー情報の取得（行ロック）\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    v_financials := v_state->'financials';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    v_total_expenses := COALESCE((v_financials->>'total_expenses')::INT, 0);\n    v_cash := COALESCE((v_financials->>'cash')::INT, 0);\n\n    -- 1. 総支出と同額を支払う\n    v_cash := v_cash - v_total_expenses;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n\n    -- 2. フラグの更新 (2回休み、Charityのリセット)\n    v_flags := v_flags\n        || jsonb_build_object('downsized_turns_left', 2)\n        || jsonb_build_object('charity_turns_left', 0);\n\n    -- 状態の更新\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_land_on_downsized END: user_id=%', p_user_id;\n    RETURN jsonb_build_object(\n        'status', 'success', \n        'message', 'リストラ（解雇）されました。総支出と同額（$' || v_total_expenses || '）を支払い、2回休みになります。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "action_land_on_downsized_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_land_on_downsized_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant RECORD;\n    v_state JSONB;\n    v_financials JSONB;\n    v_flags JSONB;\n    v_total_expenses INT;\n    v_cash INT;\n    v_user_name text;\nBEGIN\n    SELECT * INTO v_participant\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    v_financials := COALESCE(v_state->'financials', '{}'::jsonb);\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF COALESCE((v_flags->>'is_action_completed')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '既にアクションを完了しています。');\n    END IF;\n\n    v_total_expenses := COALESCE((v_financials->>'total_expenses')::INT, 0);\n    v_cash := COALESCE((v_financials->>'cash')::INT, 0);\n\n    -- 現金不足チェック\n    IF v_cash < v_total_expenses THEN\n        RETURN jsonb_build_object(\n            'status', 'error', \n            'message', '現金が足りません。銀行ローンを借りて、現金を増やしなさい。'\n        );\n    END IF;\n\n    -- 支払いとフラグ更新\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash - v_total_expenses));\n    v_flags := v_flags || jsonb_build_object(\n        'downsized_turns_left', 2,\n        'is_action_completed', true\n    );\n    v_state := v_state || jsonb_build_object('financials', v_financials, 'flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, 'リストラ（解雇）', \n        '総支出額（$' || v_total_expenses || '）を支払い、リストラを受け入れました。2回休みになります。'\n    );\n\n    RETURN jsonb_build_object('status', 'success', 'message', 'リストラ費用を支払いました。2回休みとなります。');\nEND;\n$function$\n"
  },
  {
    "function_name": "action_pay_doodad",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_pay_doodad(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_current_card JSONB;\n    v_cost INT;\n    v_cash INT;\n    v_state JSONB;\n    v_financials JSONB;\n    v_flags JSONB;\nBEGIN\n    RAISE NOTICE '[DEBUG] action_pay_doodad START: user_id=%', p_user_id;\n\n    -- 1. 部屋情報の取得と手番確認（排他ロック）\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    -- 2. 現在場に出ているカード情報の取得\n    v_current_card := v_room.game_state->'current_card';\n    IF v_current_card IS NULL OR (v_current_card->>'deck_type') != 'doodad' THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在処理中のDoodadカードが存在しません。');\n    END IF;\n\n    v_cost := COALESCE((v_current_card->>'cost')::int, 0);\n\n    -- 3. プレイヤー情報の取得（排他ロック）\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_financials := v_state->'financials';\n\n    -- 4. 状態の整合性検証\n    IF NOT COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'カードが引かれていません。');\n    END IF;\n    IF COALESCE((v_flags->>'is_action_completed')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '既にカードアクションは完了しています。');\n    END IF;\n\n    -- 5. 現金の確認と減算\n    v_cash := COALESCE((v_financials->>'cash')::int, 0);\n    IF v_cash < v_cost THEN\n        -- キャッシュフローゲームのルール上、支払えない場合は銀行から借入を行う必要がある\n        RETURN jsonb_build_object('status', 'error', 'message', '現金が不足しています。銀行ローンを借り入れてから再度実行してください。');\n    END IF;\n\n    v_cash := v_cash - v_cost;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n\n    -- 6. アクション完了フラグを更新\n    v_flags := v_flags || jsonb_build_object('is_action_completed', true);\n\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    -- 7. データベースの更新\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] action_pay_doodad SUCCESS: user_id=%, cost=%', p_user_id, v_cost;\n    RETURN jsonb_build_object('status', 'success', 'message', 'Doodadの費用（$' || v_cost || '）を支払いました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "action_pay_doodad_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.action_pay_doodad_v2(p_room_id text, p_user_id text)\n RETURNS jsonb\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_room RECORD;\n    v_participant RECORD;\n    v_current_card JSONB;\n    v_cost INT;\n    v_cash INT;\n    v_state JSONB;\n    v_financials JSONB;\n    v_flags JSONB;\n    v_user_name text; -- ログ用のユーザー名変数\nBEGIN\n    -- 1. 部屋情報の取得と手番確認（排他ロック）\n    SELECT * INTO v_room FROM rooms WHERE id = p_room_id FOR UPDATE;\n    IF v_room.current_turn_user_id != p_user_id THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在、あなたの手番ではありません。');\n    END IF;\n\n    -- 2. 現在場に出ているカード情報の取得\n    v_current_card := v_room.game_state->'current_card';\n    IF v_current_card IS NULL OR (v_current_card->>'deck_type') != 'doodad' THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在処理中のDoodadカードが存在しません。');\n    END IF;\n\n    v_cost := COALESCE((v_current_card->>'cost')::int, 0);\n\n    -- 3. プレイヤー情報の取得（排他ロック）\n    SELECT * INTO v_participant FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_state := v_participant.state;\n    \n    -- ★ 追加: プレイヤー名の取得\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    \n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_financials := v_state->'financials';\n\n    -- 4. 状態の整合性検証\n    IF NOT COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'カードが引かれていません。');\n    END IF;\n    IF COALESCE((v_flags->>'is_action_completed')::boolean, false) THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '既にカードアクションは完了しています。');\n    END IF;\n\n    -- 5. 現金の確認と減算\n    v_cash := COALESCE((v_financials->>'cash')::int, 0);\n    IF v_cash < v_cost THEN\n        -- キャッシュフローゲームのルール上、支払えない場合は銀行から借入を行う必要がある\n        RETURN jsonb_build_object('status', 'error', 'message', '現金が不足しています。銀行ローンを借り入れてから再度実行してください。');\n    END IF;\n\n    v_cash := v_cash - v_cost;\n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n\n    -- 6. アクション完了フラグを更新\n    v_flags := v_flags || jsonb_build_object('is_action_completed', true);\n\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    -- 7. データベースの更新\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ★ 追加: game_logs テーブルへの書き込み\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        '支払い',\n        'Doodadの費用（$' || v_cost || '）を支払いました。'\n    );\n\n    RETURN jsonb_build_object('status', 'success', 'message', 'Doodadの費用（$' || v_cost || '）を支払いました。');\nEND;\n$function$\n"
  },
  {
    "function_name": "borrow_bank_loan",
    "function_definition": "CREATE OR REPLACE FUNCTION public.borrow_bank_loan(p_room_id text, p_user_id text, p_amount integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_financials jsonb;\n    v_liabilities jsonb;\n    v_expenses jsonb;\n    v_flags jsonb;\n    v_cash int;\n    v_bank_loan int;\n    v_bank_loan_payment int;\n    v_total_expenses int;\n    v_total_income int;\n    v_net_cash_flow int;\n    v_interest_rate numeric := 0.10;\nBEGIN\n    RAISE NOTICE '[DEBUG] borrow_bank_loan START: user_id=%, amount=%', p_user_id, p_amount;\n\n    IF p_amount <= 0 OR p_amount % 1000 != 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '借入金額は$1,000単位で指定してください。');\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_financials := v_state->'financials';\n    v_liabilities := v_financials->'liabilities';\n    v_expenses := v_financials->'expenses';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    v_cash := COALESCE((v_financials->>'cash')::int, 0) + p_amount;\n    v_bank_loan := COALESCE((v_liabilities->>'bank_loan')::int, 0) + p_amount;\n    \n    v_bank_loan_payment := v_bank_loan * v_interest_rate;\n\n    v_liabilities := jsonb_set(v_liabilities, '{bank_loan}', to_jsonb(v_bank_loan));\n    v_expenses := jsonb_set(v_expenses, '{bank_loan_payment}', to_jsonb(v_bank_loan_payment));\n    \n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n    v_financials := jsonb_set(v_financials, '{liabilities}', v_liabilities);\n    v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n\n    -- ★修正: すべての支出項目を網羅し、キー名を修正\n    v_total_expenses := \n        COALESCE((v_expenses->>'taxes')::int, 0) +\n        COALESCE((v_expenses->>'mortgage_payment')::int, 0) +\n        COALESCE((v_expenses->>'school_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'car_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'credit_card_payment')::int, 0) +\n        COALESCE((v_expenses->>'retail_payment')::int, 0) +\n        COALESCE((v_expenses->>'other_expenses')::int, 0) +\n        COALESCE((v_expenses->>'child_expense')::int, 0) +\n        v_bank_loan_payment;\n\n    v_financials := jsonb_set(v_financials, '{total_expenses}', to_jsonb(v_total_expenses));\n\n    v_total_income := COALESCE((v_financials->>'total_income')::int, 0);\n    v_net_cash_flow := v_total_income - v_total_expenses;\n    v_financials := jsonb_set(v_financials, '{net_cash_flow}', to_jsonb(v_net_cash_flow));\n\n    v_flags := v_flags \n        || jsonb_build_object('is_calculating', true)\n        || jsonb_build_object('is_negative_cash_flow', (v_net_cash_flow < 0));\n\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] borrow_bank_loan END: new_cash=%, new_loan=%', v_cash, v_bank_loan;\n    RETURN jsonb_build_object('status', 'success', 'message', '銀行から借入を行いました。財務諸表を再計算してください。');\nEND;\n$function$\n"
  },
  {
    "function_name": "borrow_bank_loan_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.borrow_bank_loan_v2(p_room_id text, p_user_id text, p_amount integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_financials jsonb;\n    v_liabilities jsonb;\n    v_expenses jsonb;\n    v_flags jsonb;\n    v_cash int;\n    v_bank_loan int;\n    v_bank_loan_payment int;\n    v_total_expenses int;\n    v_total_income int;\n    v_net_cash_flow int;\n    v_interest_rate numeric := 0.10;\n    v_user_name text; \nBEGIN\n    IF p_amount <= 0 OR p_amount % 1000 != 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '借入金額は$1,000単位で指定してください。');\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n\n    v_financials := v_state->'financials';\n    v_liabilities := v_financials->'liabilities';\n    v_expenses := v_financials->'expenses';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    v_cash := COALESCE((v_financials->>'cash')::int, 0) + p_amount;\n    v_bank_loan := COALESCE((v_liabilities->>'bank_loan')::int, 0) + p_amount;\n    \n    v_bank_loan_payment := v_bank_loan * v_interest_rate;\n\n    v_liabilities := jsonb_set(v_liabilities, '{bank_loan}', to_jsonb(v_bank_loan));\n    v_expenses := jsonb_set(v_expenses, '{bank_loan_payment}', to_jsonb(v_bank_loan_payment));\n    \n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n    v_financials := jsonb_set(v_financials, '{liabilities}', v_liabilities);\n    v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n\n    v_total_expenses := \n        COALESCE((v_expenses->>'taxes')::int, 0) +\n        COALESCE((v_expenses->>'mortgage_payment')::int, 0) +\n        COALESCE((v_expenses->>'school_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'car_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'credit_card_payment')::int, 0) +\n        COALESCE((v_expenses->>'retail_payment')::int, 0) +\n        COALESCE((v_expenses->>'other_expenses')::int, 0) +\n        COALESCE((v_expenses->>'child_expense')::int, 0) +\n        v_bank_loan_payment;\n\n    v_financials := jsonb_set(v_financials, '{total_expenses}', to_jsonb(v_total_expenses));\n\n    v_total_income := COALESCE((v_financials->>'total_income')::int, 0);\n    v_net_cash_flow := v_total_income - v_total_expenses;\n    v_financials := jsonb_set(v_financials, '{net_cash_flow}', to_jsonb(v_net_cash_flow));\n\n    v_flags := v_flags \n        || jsonb_build_object('is_calculating', true)\n        || jsonb_build_object('is_negative_cash_flow', (v_net_cash_flow < 0));\n\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 🌟修正: game_logs に再計算の指示を追加\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, '銀行ローン借入',\n        '銀行から $' || p_amount || ' のローンを借り入れました。財務諸表を再計算してください。'\n    );\n\n    RETURN jsonb_build_object('status', 'success', 'message', '銀行から借入を行いました。財務諸表を再計算してください。');\nEND;\n$function$\n"
  },
  {
    "function_name": "claim_paycheck",
    "function_definition": "CREATE OR REPLACE FUNCTION public.claim_paycheck(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_flags jsonb;\n    v_pending_paydays int;\n    v_net_cash_flow int;\n    v_total_payout int;\nBEGIN\n    RAISE NOTICE '[DEBUG] claim_paycheck START: user_id=%', p_user_id;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN \n        RAISE EXCEPTION 'Invalid user or room.'; \n    END IF;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_pending_paydays := COALESCE((v_flags->>'pending_paydays')::int, 0);\n\n    RAISE NOTICE '[DEBUG] claim_paycheck: pending_paydays=%', v_pending_paydays;\n\n    IF v_pending_paydays <= 0 THEN\n        RAISE EXCEPTION '現在、給料受け取りの権利はありません。';\n    END IF;\n\n    v_net_cash_flow := COALESCE((v_state->'financials'->>'net_cash_flow')::int, 0);\n    v_total_payout := v_net_cash_flow * v_pending_paydays;\n\n    -- フラグ状態の取得と更新。受け取ったので pending_paydays を 0 にリセット\n    v_flags := v_flags\n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('pending_paydays', 0);\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object(\n        'financials', v_state->'financials' || jsonb_build_object(\n            'cash', COALESCE((v_state->'financials'->>'cash')::int, 0) + v_total_payout\n        ),\n        'calculation_phase', 'none',\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] claim_paycheck END: total_payout=%', v_total_payout;\nEND;\n$function$\n"
  },
  {
    "function_name": "claim_paycheck_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.claim_paycheck_v2(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_flags jsonb;\n    v_pending_paydays int;\n    v_net_cash_flow int;\n    v_total_payout int;\n    v_user_name text; -- ログ用のユーザー名変数\nBEGIN\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN \n        RAISE EXCEPTION 'Invalid user or room.'; \n    END IF;\n\n    -- ユーザー名の取得\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_pending_paydays := COALESCE((v_flags->>'pending_paydays')::int, 0);\n\n    IF v_pending_paydays <= 0 THEN\n        RAISE EXCEPTION '現在、給料受け取りの権利はありません。';\n    END IF;\n\n    v_net_cash_flow := COALESCE((v_state->'financials'->>'net_cash_flow')::int, 0);\n    v_total_payout := v_net_cash_flow * v_pending_paydays;\n\n    -- フラグ状態の取得と更新。受け取ったので pending_paydays を 0 にリセット\n    v_flags := v_flags\n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('pending_paydays', 0);\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object(\n        'financials', v_state->'financials' || jsonb_build_object(\n            'cash', COALESCE((v_state->'financials'->>'cash')::int, 0) + v_total_payout\n        ),\n        'calculation_phase', 'none',\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ★ 追加: game_logs テーブルへの書き込み\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        'Payday（給料日）',\n        '給料（$' || v_total_payout || '）を受け取りました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "complete_card_action",
    "function_definition": "CREATE OR REPLACE FUNCTION public.complete_card_action(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_state jsonb;\n    v_flags jsonb;\nBEGIN\n    RAISE NOTICE '[DEBUG] complete_card_action START: user_id=%', p_user_id;\n\n    -- 1. 部屋情報の取得と手番の検証（行ロック）\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms\n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    -- 2. プレイヤー状態の取得（行ロック）\n    SELECT state INTO v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF v_state IS NULL THEN\n        RAISE EXCEPTION '指定されたプレイヤーが存在しません。';\n    END IF;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    -- 3. 状態の検証：既にアクションが完了している場合は弾く\n    IF COALESCE((v_flags->>'is_action_completed')::boolean, false) THEN\n        RAISE EXCEPTION '既にアクションは完了しています。';\n    END IF;\n\n    -- 4. フラグの更新\n    v_flags := v_flags || jsonb_build_object('is_action_completed', true);\n\n    -- 5. データ整合性を保証した状態でのアトミックな更新\n    UPDATE participants\n    SET state = v_state || jsonb_build_object('flags', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] complete_card_action END: user_id=%', p_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "complete_card_action_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.complete_card_action_v2(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_state jsonb;\n    v_flags jsonb;\n    v_user_name text; -- ログ用のユーザー名変数\nBEGIN\n    -- 1. 部屋情報の取得と手番の検証（行ロック）\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms\n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    -- 2. プレイヤー状態の取得（行ロック）\n    SELECT state INTO v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF v_state IS NULL THEN\n        RAISE EXCEPTION '指定されたプレイヤーが存在しません。';\n    END IF;\n\n    -- ★ 追加: ユーザー名の取得\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    -- 3. 状態の検証：既にアクションが完了している場合は弾く\n    IF COALESCE((v_flags->>'is_action_completed')::boolean, false) THEN\n        RAISE EXCEPTION '既にアクションは完了しています。';\n    END IF;\n\n    -- 4. フラグの更新\n    v_flags := v_flags || jsonb_build_object('is_action_completed', true);\n\n    -- 5. データ整合性を保証した状態でのアトミックな更新\n    UPDATE participants\n    SET state = v_state || jsonb_build_object('flags', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ★ 追加: game_logs テーブルへの書き込み\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        'アクション完了',\n        'カードに対するアクションを完了しました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "declare_game_over",
    "function_definition": "CREATE OR REPLACE FUNCTION public.declare_game_over(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    RAISE NOTICE '[DEBUG] declare_game_over START: user_id=%', p_user_id;\n\n    UPDATE participants \n    SET state = jsonb_set(state, '{game_phase}', '\"game_over\"')\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] declare_game_over END: user_id=%', p_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "declare_game_over_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.declare_game_over_v2(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_user_name text;\nBEGIN\n    -- ログ記録用にユーザー名を取得\n    SELECT COALESCE(state->>'name', 'Unknown') INTO v_user_name\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ステータスをゲームオーバーに更新\n    UPDATE participants \n    SET state = jsonb_set(state, '{game_phase}', '\"game_over\"')\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- ログの記録\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, 'ゲーム終了',\n        'ゲームオーバーとなりました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "draw_card",
    "function_definition": "CREATE OR REPLACE FUNCTION public.draw_card(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_state jsonb;\n    v_flags jsonb;\nBEGIN\n    RAISE NOTICE '[DEBUG] draw_card START: user_id=%', p_user_id;\n\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms\n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    SELECT state INTO v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF v_state IS NULL THEN\n        RAISE EXCEPTION '指定されたプレイヤーが存在しません。';\n    END IF;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'サイコロを振る前にカードを引くことはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RAISE EXCEPTION 'すでにカードを引いています。重複して引くことはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '計算フェーズ中（給料処理など）のため、カードを引くことはできません。';\n    END IF;\n\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object('flags', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] draw_card END: is_card_drawn set to true';\nEND;\n$function$\n"
  },
  {
    "function_name": "draw_card_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.draw_card_v2(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_state jsonb;\n    v_flags jsonb;\n    v_user_name text; -- ログ用のユーザー名変数\nBEGIN\n    SELECT current_turn_user_id INTO v_current_turn_user_id\n    FROM rooms\n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    SELECT state INTO v_state\n    FROM participants\n    WHERE room_id = p_room_id AND user_id = p_user_id\n    FOR UPDATE;\n\n    IF v_state IS NULL THEN\n        RAISE EXCEPTION '指定されたプレイヤーが存在しません。';\n    END IF;\n\n    -- ユーザー名の取得\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    IF NOT COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'サイコロを振る前にカードを引くことはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_card_drawn')::boolean, false) THEN\n        RAISE EXCEPTION 'すでにカードを引いています。重複して引くことはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '計算フェーズ中（給料処理など）のため、カードを引くことはできません。';\n    END IF;\n\n    v_flags := v_flags || jsonb_build_object('is_card_drawn', true);\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object('flags', v_flags)\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- game_logs テーブルへのINSERT処理\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        'アクション進行',\n        'カードドローのアクションを開始しました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "fn_insert_game_log",
    "function_definition": "CREATE OR REPLACE FUNCTION public.fn_insert_game_log(p_room_id text, p_target text, p_title text, p_body text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    INSERT INTO public.game_logs (room_id, target, title, body)\n    VALUES (p_room_id, p_target, p_title, p_body);\nEND;\n$function$\n"
  },
  {
    "function_name": "force_set_turn",
    "function_definition": "CREATE OR REPLACE FUNCTION public.force_set_turn(p_room_id text, p_target_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_participant_exists boolean;\nBEGIN\n    RAISE NOTICE '[DEBUG] force_set_turn START: room_id=%, target_user_id=%', p_room_id, p_target_user_id;\n\n    -- 対象のプレイヤーがその部屋に存在するか確認\n    SELECT EXISTS (\n        SELECT 1 FROM participants \n        WHERE room_id = p_room_id AND user_id = p_target_user_id\n    ) INTO v_participant_exists;\n\n    IF NOT v_participant_exists THEN\n        RAISE EXCEPTION '指定されたプレイヤーはこの部屋に存在しません。';\n    END IF;\n\n    -- 部屋の現在手番を強制的に更新\n    UPDATE rooms \n    SET current_turn_user_id = p_target_user_id \n    WHERE id = p_room_id;\n\n    RAISE NOTICE '[DEBUG] force_set_turn END: Turn successfully set to %', p_target_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "kick_participant",
    "function_definition": "CREATE OR REPLACE FUNCTION public.kick_participant(p_room_id text, p_target_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_next_id text;\n    v_participant_count int;\nBEGIN\n    RAISE NOTICE '[DEBUG] kick_participant START: room_id=%, target_user_id=%', p_room_id, p_target_user_id;\n\n    -- 部屋の情報をロックして取得\n    SELECT current_turn_user_id INTO v_current_turn_user_id \n    FROM rooms \n    WHERE id = p_room_id FOR UPDATE;\n\n    -- 対象プレイヤーが現在手番かどうかを判定\n    IF v_current_turn_user_id = p_target_user_id THEN\n        -- 対象者以外の残りプレイヤー数を取得\n        SELECT COUNT(*) INTO v_participant_count \n        FROM participants \n        WHERE room_id = p_room_id AND user_id != p_target_user_id;\n\n        IF v_participant_count > 0 THEN\n            -- 次の順番のプレイヤーを取得\n            SELECT user_id INTO v_next_id \n            FROM participants\n            WHERE room_id = p_room_id \n              AND id > (SELECT id FROM participants WHERE room_id = p_room_id AND user_id = p_target_user_id)\n              AND user_id != p_target_user_id\n            ORDER BY id ASC LIMIT 1;\n\n            -- 対象者がリストの最後尾だった場合、先頭のプレイヤーを次の手番にする\n            IF v_next_id IS NULL THEN\n                SELECT user_id INTO v_next_id \n                FROM participants \n                WHERE room_id = p_room_id \n                  AND user_id != p_target_user_id\n                ORDER BY id ASC LIMIT 1;\n            END IF;\n\n            -- 部屋の現在手番を更新\n            UPDATE rooms \n            SET current_turn_user_id = v_next_id \n            WHERE id = p_room_id;\n            \n            RAISE NOTICE '[DEBUG] kick_participant: Turn moved to %', v_next_id;\n        ELSE\n            -- 残りのプレイヤーが誰もいなくなる場合、部屋のステータスをリセット\n            UPDATE rooms \n            SET current_turn_user_id = NULL,\n                game_state = jsonb_build_object('status', 'waiting')\n            WHERE id = p_room_id;\n            \n            RAISE NOTICE '[DEBUG] kick_participant: No players left, room reset to waiting';\n        END IF;\n    END IF;\n\n    -- 参加者レコードの削除\n    DELETE FROM participants \n    WHERE room_id = p_room_id AND user_id = p_target_user_id;\n\n    RAISE NOTICE '[DEBUG] kick_participant END';\nEND;\n$function$\n"
  },
  {
    "function_name": "merge_participant_state",
    "function_definition": "CREATE OR REPLACE FUNCTION public.merge_participant_state(target_user_id text, state_patch jsonb)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    RAISE NOTICE '[DEBUG] merge_participant_state: target_user_id=%', target_user_id;\n    UPDATE participants \n    SET state = COALESCE(state, '{}'::jsonb) || state_patch \n    WHERE user_id = target_user_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "pass_and_end_turn",
    "function_definition": "CREATE OR REPLACE FUNCTION public.pass_and_end_turn(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_next_id text;\n    v_state jsonb;\n    v_flags jsonb;\n    v_net_cash_flow int;\n    v_downsized_turns_left int;\n    \n    v_loop_count int := 0;\n    v_max_participants int;\n    v_temp_id text;\n    v_temp_state jsonb;\n    v_temp_flags jsonb;\n    v_temp_downsized int;\nBEGIN\n    RAISE NOTICE '[DEBUG] pass_and_end_turn START: user_id=%', p_user_id;\n\n    SELECT current_turn_user_id INTO v_current_turn_user_id \n    FROM rooms \n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_downsized_turns_left := COALESCE((v_flags->>'downsized_turns_left')::int, 0);\n\n    -- ★修正: サイコロを振ったターン（解雇マスに止まったターン）は休み回数を消費しない\n    IF COALESCE((v_flags->>'has_rolled_dice')::boolean, false) = true THEN\n        -- 通常の手番終了。何もしない\n    ELSIF v_downsized_turns_left > 0 THEN\n        -- 自動スキップが漏れた場合の、休み中の手動スキップ処理\n        v_downsized_turns_left := v_downsized_turns_left - 1;\n    ELSE\n        RAISE EXCEPTION 'サイコロを振る前にターンを終了することはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '計算フェーズ中のため、ターンを終了することはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_negative_cash_flow')::boolean, false) AND COALESCE((v_flags->>'pending_paydays')::int, 0) > 0 THEN\n        RAISE EXCEPTION 'マイナスキャッシュフローの未処理問題（給料日の支払い）があるため、ターンを終了することはできません。';\n    END IF;\n\n    v_net_cash_flow := COALESCE((v_state->'financials'->>'net_cash_flow')::int, 0);\n\n    v_flags := jsonb_build_object(\n        'has_rolled_dice', false,\n        'is_card_drawn', false,\n        'is_action_completed', false,\n        'is_calculating', false,\n        'is_negative_cash_flow', (v_net_cash_flow < 0),\n        'charity_turns_left', COALESCE((v_flags->>'charity_turns_left')::int, 0),\n        'downsized_turns_left', v_downsized_turns_left,\n        'pending_paydays', 0 \n    );\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object(\n        'last_dice', 0,\n        'calculation_phase', 'none',\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 手番のスキップ判定ループ (他のプレイヤーの休み自動消化)\n    SELECT COUNT(*) INTO v_max_participants FROM participants WHERE room_id = p_room_id;\n    v_temp_id := p_user_id;\n    \n    LOOP\n        SELECT user_id INTO v_next_id \n        FROM participants\n        WHERE room_id = p_room_id \n          AND id > (SELECT id FROM participants WHERE room_id = p_room_id AND user_id = v_temp_id)\n        ORDER BY id ASC LIMIT 1;\n\n        IF v_next_id IS NULL THEN\n            SELECT user_id INTO v_next_id \n            FROM participants \n            WHERE room_id = p_room_id \n            ORDER BY id ASC LIMIT 1;\n        END IF;\n\n        SELECT state INTO v_temp_state \n        FROM participants \n        WHERE room_id = p_room_id AND user_id = v_next_id \n        FOR UPDATE;\n        \n        v_temp_flags := COALESCE(v_temp_state->'flags', '{}'::jsonb);\n        v_temp_downsized := COALESCE((v_temp_flags->>'downsized_turns_left')::int, 0);\n\n        IF v_temp_downsized > 0 THEN\n            v_temp_downsized := v_temp_downsized - 1;\n            v_temp_flags := jsonb_set(v_temp_flags, '{downsized_turns_left}', to_jsonb(v_temp_downsized));\n            \n            UPDATE participants\n            SET state = jsonb_set(v_temp_state, '{flags}', v_temp_flags)\n            WHERE room_id = p_room_id AND user_id = v_next_id;\n            \n            RAISE NOTICE '[DEBUG] pass_and_end_turn: user % is downsized, remaining=% (SKIPPED)', v_next_id, v_temp_downsized;\n            \n            v_temp_id := v_next_id;\n            v_loop_count := v_loop_count + 1;\n            \n            IF v_loop_count >= v_max_participants THEN\n                RAISE NOTICE '[DEBUG] pass_and_end_turn: 全員が休みです。';\n                EXIT;\n            END IF;\n        ELSE\n            EXIT;\n        END IF;\n    END LOOP;\n\n    UPDATE rooms \n    SET current_turn_user_id = v_next_id \n    WHERE id = p_room_id;\n\n    RAISE NOTICE '[DEBUG] pass_and_end_turn END: next_user_id=%', v_next_id;\nEND;\n$function$\n"
  },
  {
    "function_name": "pass_and_end_turn_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.pass_and_end_turn_v2(p_room_id text, p_user_id text)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_current_turn_user_id text;\n    v_next_id text;\n    v_state jsonb;\n    v_flags jsonb;\n    v_net_cash_flow int;\n    v_downsized_turns_left int;\n    \n    v_loop_count int := 0;\n    v_max_participants int;\n    v_temp_id text;\n    v_temp_state jsonb;\n    v_temp_flags jsonb;\n    v_temp_downsized int;\n    v_user_name text; -- ★ ログ用のユーザー名変数\nBEGIN\n    SELECT current_turn_user_id INTO v_current_turn_user_id \n    FROM rooms \n    WHERE id = p_room_id FOR UPDATE;\n\n    IF v_current_turn_user_id IS NULL OR v_current_turn_user_id != p_user_id THEN\n        RAISE EXCEPTION '現在、指定されたユーザーの手番ではありません。';\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    -- ★ 追加: ユーザー名の取得\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_downsized_turns_left := COALESCE((v_flags->>'downsized_turns_left')::int, 0);\n\n    -- サイコロを振ったターン（解雇マスに止まったターン）は休み回数を消費しない\n    IF COALESCE((v_flags->>'has_rolled_dice')::boolean, false) = true THEN\n        -- 通常の手番終了。何もしない\n    ELSIF v_downsized_turns_left > 0 THEN\n        -- 自動スキップが漏れた場合の、休み中の手動スキップ処理\n        v_downsized_turns_left := v_downsized_turns_left - 1;\n    ELSE\n        RAISE EXCEPTION 'サイコロを振る前にターンを終了することはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '計算フェーズ中のため、ターンを終了することはできません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_negative_cash_flow')::boolean, false) AND COALESCE((v_flags->>'pending_paydays')::int, 0) > 0 THEN\n        RAISE EXCEPTION 'マイナスキャッシュフローの未処理問題（給料日の支払い）があるため、ターンを終了することはできません。';\n    END IF;\n\n    v_net_cash_flow := COALESCE((v_state->'financials'->>'net_cash_flow')::int, 0);\n\n    v_flags := jsonb_build_object(\n        'has_rolled_dice', false,\n        'is_card_drawn', false,\n        'is_action_completed', false,\n        'is_calculating', false,\n        'is_negative_cash_flow', (v_net_cash_flow < 0),\n        'charity_turns_left', COALESCE((v_flags->>'charity_turns_left')::int, 0),\n        'downsized_turns_left', v_downsized_turns_left,\n        'pending_paydays', 0 \n    );\n\n    UPDATE participants\n    SET state = v_state || jsonb_build_object(\n        'last_dice', 0,\n        'calculation_phase', 'none',\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 手番のスキップ判定ループ (他のプレイヤーの休み自動消化)\n    SELECT COUNT(*) INTO v_max_participants FROM participants WHERE room_id = p_room_id;\n    v_temp_id := p_user_id;\n    \n    LOOP\n        SELECT user_id INTO v_next_id \n        FROM participants\n        WHERE room_id = p_room_id \n          AND id > (SELECT id FROM participants WHERE room_id = p_room_id AND user_id = v_temp_id)\n        ORDER BY id ASC LIMIT 1;\n\n        IF v_next_id IS NULL THEN\n            SELECT user_id INTO v_next_id \n            FROM participants \n            WHERE room_id = p_room_id \n            ORDER BY id ASC LIMIT 1;\n        END IF;\n\n        SELECT state INTO v_temp_state \n        FROM participants \n        WHERE room_id = p_room_id AND user_id = v_next_id \n        FOR UPDATE;\n        \n        v_temp_flags := COALESCE(v_temp_state->'flags', '{}'::jsonb);\n        v_temp_downsized := COALESCE((v_temp_flags->>'downsized_turns_left')::int, 0);\n\n        IF v_temp_downsized > 0 THEN\n            v_temp_downsized := v_temp_downsized - 1;\n            v_temp_flags := jsonb_set(v_temp_flags, '{downsized_turns_left}', to_jsonb(v_temp_downsized));\n            \n            UPDATE participants\n            SET state = jsonb_set(v_temp_state, '{flags}', v_temp_flags)\n            WHERE room_id = p_room_id AND user_id = v_next_id;\n            \n            v_temp_id := v_next_id;\n            v_loop_count := v_loop_count + 1;\n            \n            IF v_loop_count >= v_max_participants THEN\n                EXIT;\n            END IF;\n        ELSE\n            EXIT;\n        END IF;\n    END LOOP;\n\n    UPDATE rooms \n    SET current_turn_user_id = v_next_id \n    WHERE id = p_room_id;\n\n    -- ★ 追加: game_logs テーブルへの書き込み\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        '手番終了',\n        '手番を終了し、次のプレイヤーにターンを移行しました。'\n    );\nEND;\n$function$\n"
  },
  {
    "function_name": "repay_bank_loan",
    "function_definition": "CREATE OR REPLACE FUNCTION public.repay_bank_loan(p_room_id text, p_user_id text, p_amount integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_financials jsonb;\n    v_liabilities jsonb;\n    v_expenses jsonb;\n    v_flags jsonb;\n    v_cash int;\n    v_bank_loan int;\n    v_bank_loan_payment int;\n    v_total_expenses int;\n    v_total_income int;\n    v_net_cash_flow int;\n    v_interest_rate numeric := 0.10;\nBEGIN\n    RAISE NOTICE '[DEBUG] repay_bank_loan START: user_id=%, amount=%', p_user_id, p_amount;\n\n    IF p_amount <= 0 OR p_amount % 1000 != 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '返済金額は$1,000単位で指定してください。');\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_financials := v_state->'financials';\n    v_liabilities := v_financials->'liabilities';\n    v_expenses := v_financials->'expenses';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    v_cash := COALESCE((v_financials->>'cash')::int, 0);\n    v_bank_loan := COALESCE((v_liabilities->>'bank_loan')::int, 0);\n\n    IF v_bank_loan < p_amount THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在の借入額を上回る返済はできません。');\n    END IF;\n\n    IF v_cash < p_amount THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現金が不足しています。');\n    END IF;\n\n    v_cash := v_cash - p_amount;\n    v_bank_loan := v_bank_loan - p_amount;\n    v_bank_loan_payment := v_bank_loan * v_interest_rate;\n\n    v_liabilities := jsonb_set(v_liabilities, '{bank_loan}', to_jsonb(v_bank_loan));\n    v_expenses := jsonb_set(v_expenses, '{bank_loan_payment}', to_jsonb(v_bank_loan_payment));\n    \n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n    v_financials := jsonb_set(v_financials, '{liabilities}', v_liabilities);\n    v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n\n    -- ★修正: すべての支出項目を網羅し、キー名を修正\n    v_total_expenses := \n        COALESCE((v_expenses->>'taxes')::int, 0) +\n        COALESCE((v_expenses->>'mortgage_payment')::int, 0) +\n        COALESCE((v_expenses->>'school_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'car_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'credit_card_payment')::int, 0) +\n        COALESCE((v_expenses->>'retail_payment')::int, 0) +\n        COALESCE((v_expenses->>'other_expenses')::int, 0) +\n        COALESCE((v_expenses->>'child_expense')::int, 0) +\n        v_bank_loan_payment;\n\n    v_financials := jsonb_set(v_financials, '{total_expenses}', to_jsonb(v_total_expenses));\n\n    v_total_income := COALESCE((v_financials->>'total_income')::int, 0);\n    v_net_cash_flow := v_total_income - v_total_expenses;\n    v_financials := jsonb_set(v_financials, '{net_cash_flow}', to_jsonb(v_net_cash_flow));\n\n    v_flags := v_flags \n        || jsonb_build_object('is_calculating', true)\n        || jsonb_build_object('is_negative_cash_flow', (v_net_cash_flow < 0));\n\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] repay_bank_loan END: new_cash=%, new_loan=%', v_cash, v_bank_loan;\n    RETURN jsonb_build_object('status', 'success', 'message', '銀行ローンの返済を行いました。財務諸表を再計算してください。');\nEND;\n$function$\n"
  },
  {
    "function_name": "repay_bank_loan_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.repay_bank_loan_v2(p_room_id text, p_user_id text, p_amount integer)\n RETURNS jsonb\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_financials jsonb;\n    v_liabilities jsonb;\n    v_expenses jsonb;\n    v_flags jsonb;\n    v_cash int;\n    v_bank_loan int;\n    v_bank_loan_payment int;\n    v_total_expenses int;\n    v_total_income int;\n    v_net_cash_flow int;\n    v_interest_rate numeric := 0.10;\n    v_user_name text; \nBEGIN\n    IF p_amount <= 0 OR p_amount % 1000 != 0 THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '返済金額は$1,000単位で指定してください。');\n    END IF;\n\n    SELECT state INTO v_state \n    FROM participants \n    WHERE room_id = p_room_id AND user_id = p_user_id \n    FOR UPDATE;\n\n    IF NOT FOUND THEN\n        RETURN jsonb_build_object('status', 'error', 'message', 'プレイヤーが見つかりません。');\n    END IF;\n\n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n\n    v_financials := v_state->'financials';\n    v_liabilities := v_financials->'liabilities';\n    v_expenses := v_financials->'expenses';\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n\n    v_cash := COALESCE((v_financials->>'cash')::int, 0);\n    v_bank_loan := COALESCE((v_liabilities->>'bank_loan')::int, 0);\n\n    IF v_bank_loan < p_amount THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現在の借入額を上回る返済はできません。');\n    END IF;\n\n    IF v_cash < p_amount THEN\n        RETURN jsonb_build_object('status', 'error', 'message', '現金が不足しています。');\n    END IF;\n\n    v_cash := v_cash - p_amount;\n    v_bank_loan := v_bank_loan - p_amount;\n    v_bank_loan_payment := v_bank_loan * v_interest_rate;\n\n    v_liabilities := jsonb_set(v_liabilities, '{bank_loan}', to_jsonb(v_bank_loan));\n    v_expenses := jsonb_set(v_expenses, '{bank_loan_payment}', to_jsonb(v_bank_loan_payment));\n    \n    v_financials := jsonb_set(v_financials, '{cash}', to_jsonb(v_cash));\n    v_financials := jsonb_set(v_financials, '{liabilities}', v_liabilities);\n    v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n\n    v_total_expenses := \n        COALESCE((v_expenses->>'taxes')::int, 0) +\n        COALESCE((v_expenses->>'mortgage_payment')::int, 0) +\n        COALESCE((v_expenses->>'school_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'car_loan_payment')::int, 0) +\n        COALESCE((v_expenses->>'credit_card_payment')::int, 0) +\n        COALESCE((v_expenses->>'retail_payment')::int, 0) +\n        COALESCE((v_expenses->>'other_expenses')::int, 0) +\n        COALESCE((v_expenses->>'child_expense')::int, 0) +\n        v_bank_loan_payment;\n\n    v_financials := jsonb_set(v_financials, '{total_expenses}', to_jsonb(v_total_expenses));\n\n    v_total_income := COALESCE((v_financials->>'total_income')::int, 0);\n    v_net_cash_flow := v_total_income - v_total_expenses;\n    v_financials := jsonb_set(v_financials, '{net_cash_flow}', to_jsonb(v_net_cash_flow));\n\n    v_flags := v_flags \n        || jsonb_build_object('is_calculating', true)\n        || jsonb_build_object('is_negative_cash_flow', (v_net_cash_flow < 0));\n\n    v_state := v_state \n        || jsonb_build_object('financials', v_financials)\n        || jsonb_build_object('flags', v_flags);\n\n    UPDATE participants\n    SET state = v_state\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 🌟修正: game_logs に再計算の指示を追加\n    PERFORM public.fn_insert_game_log(\n        p_room_id, v_user_name, '銀行ローン返済',\n        '銀行に $' || p_amount || ' のローンを返済しました。財務諸表を再計算してください。'\n    );\n\n    RETURN jsonb_build_object('status', 'success', 'message', '銀行ローンの返済を行いました。財務諸表を再計算してください。');\nEND;\n$function$\n"
  },
  {
    "function_name": "roll_dice_and_move",
    "function_definition": "CREATE OR REPLACE FUNCTION public.roll_dice_and_move(p_room_id text, p_user_id text, p_dice_count integer DEFAULT 1)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_flags jsonb;\n    v_dice int := 0;\n    v_pos int;\n    v_payday_count int := 0;\n    v_charity_turns_left int;\n    v_pending_paydays int;\nBEGIN\n    RAISE NOTICE '[DEBUG] roll_dice_and_move START: user_id=%, dice_count=%', p_user_id, p_dice_count;\n\n    SELECT state INTO v_state FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    \n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_charity_turns_left := COALESCE((v_flags->>'charity_turns_left')::int, 0);\n    v_pending_paydays := COALESCE((v_flags->>'pending_paydays')::int, 0);\n\n    IF COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'すでにサイコロを振っています。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '現在計算フェーズ中（または未処理のイベントあり）のため、サイコロを振れません。';\n    END IF;\n\n    -- ★追加: 休み残がある場合はサイコロ操作を強制ブロック\n    IF COALESCE((v_flags->>'downsized_turns_left')::int, 0) > 0 THEN\n        RAISE EXCEPTION 'リストラ（休み）中のため、サイコロを振ることはできません。ターンを終了してください。';\n    END IF;\n\n    IF p_dice_count < 1 OR p_dice_count > 2 THEN\n        RAISE EXCEPTION 'サイコロの数は1または2のみ指定可能です。';\n    END IF;\n\n    IF p_dice_count = 2 AND v_charity_turns_left <= 0 THEN\n        RAISE EXCEPTION '寄付の権利がないため、サイコロを2個振ることはできません。';\n    END IF;\n\n    FOR i IN 1..p_dice_count LOOP\n        v_dice := v_dice + (floor(random() * 6) + 1)::int;\n    END LOOP;\n    v_pos := COALESCE((v_state->>'position')::int, 0);\n\n    FOR i IN 1..v_dice LOOP\n        IF ((v_pos + i) % 24) IN (0, 5, 11, 18) THEN \n            v_payday_count := v_payday_count + 1; \n        END IF;\n    END LOOP;\n\n    IF v_charity_turns_left > 0 THEN\n        v_charity_turns_left := v_charity_turns_left - 1;\n    END IF;\n    \n    v_pending_paydays := v_pending_paydays + v_payday_count;\n\n    v_flags := v_flags \n        || jsonb_build_object('has_rolled_dice', true)\n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('charity_turns_left', v_charity_turns_left)\n        || jsonb_build_object('pending_paydays', v_pending_paydays);\n\n    UPDATE participants \n    SET state = v_state || jsonb_build_object(\n        'position', (v_pos + v_dice) % 24,\n        'last_dice', v_dice,\n        'calculation_phase', CASE WHEN v_pending_paydays > 0 THEN 'payday' ELSE 'none' END,\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    RAISE NOTICE '[DEBUG] roll_dice_and_move END: new_pos=%, payday_count=%, remaining_charity=%', ((v_pos + v_dice) % 24), v_payday_count, v_charity_turns_left;\nEND;\n$function$\n"
  },
  {
    "function_name": "roll_dice_and_move_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.roll_dice_and_move_v2(p_room_id text, p_user_id text, p_dice_count integer DEFAULT 1)\n RETURNS void\n LANGUAGE plpgsql\nAS $function$\nDECLARE\n    v_state jsonb;\n    v_financials jsonb;\n    v_expenses jsonb;\n    v_flags jsonb;\n    v_dice int := 0;\n    v_pos int;\n    v_new_pos int;\n    v_payday_count int := 0;\n    v_charity_turns_left int;\n    v_pending_paydays int;\n    v_user_name text;\n    v_cell_name text;\n    v_cell_msg text;\n\n    -- Babyマス計算用\n    v_children_count int;\n    v_per_child_expense int;\n    v_total_expenses int;\n    v_total_income int;\nBEGIN\n    SELECT state INTO v_state FROM participants WHERE room_id = p_room_id AND user_id = p_user_id FOR UPDATE;\n    \n    v_user_name := COALESCE(v_state->>'name', 'Unknown');\n    v_flags := COALESCE(v_state->'flags', '{}'::jsonb);\n    v_financials := COALESCE(v_state->'financials', '{}'::jsonb);\n    v_expenses := COALESCE(v_financials->'expenses', '{}'::jsonb);\n    v_charity_turns_left := COALESCE((v_flags->>'charity_turns_left')::int, 0);\n    v_pending_paydays := COALESCE((v_flags->>'pending_paydays')::int, 0);\n\n    IF COALESCE((v_flags->>'has_rolled_dice')::boolean, false) THEN\n        RAISE EXCEPTION 'すでにサイコロを振っています。';\n    END IF;\n\n    IF COALESCE((v_flags->>'is_calculating')::boolean, false) THEN\n        RAISE EXCEPTION '現在計算フェーズ中（または未処理のイベントあり）のため、サイコロを振れません。';\n    END IF;\n\n    IF COALESCE((v_flags->>'downsized_turns_left')::int, 0) > 0 THEN\n        RAISE EXCEPTION 'リストラ（休み）中のため、サイコロを振ることはできません。ターンを終了してください。';\n    END IF;\n\n    IF p_dice_count < 1 OR p_dice_count > 2 THEN\n        RAISE EXCEPTION 'サイコロの数は1または2のみ指定可能です。';\n    END IF;\n\n    IF p_dice_count = 2 AND v_charity_turns_left <= 0 THEN\n        RAISE EXCEPTION '寄付の権利がないため、サイコロを2個振ることはできません。';\n    END IF;\n\n    FOR i IN 1..p_dice_count LOOP\n        v_dice := v_dice + (floor(random() * 6) + 1)::int;\n    END LOOP;\n    \n    v_pos := COALESCE((v_state->>'position')::int, 0);\n    v_new_pos := (v_pos + v_dice) % 24;\n\n    FOR i IN 1..v_dice LOOP\n        IF ((v_pos + i) % 24) IN (0, 5, 11, 18) THEN \n            v_payday_count := v_payday_count + 1; \n        END IF;\n    END LOOP;\n\n    IF v_charity_turns_left > 0 THEN\n        v_charity_turns_left := v_charity_turns_left - 1;\n    END IF;\n    \n    v_pending_paydays := v_pending_paydays + v_payday_count;\n\n    -- ここで一度基本のフラグをセットします\n    v_flags := v_flags \n        || jsonb_build_object('has_rolled_dice', true)\n        || jsonb_build_object('is_calculating', false)\n        || jsonb_build_object('charity_turns_left', v_charity_turns_left)\n        || jsonb_build_object('pending_paydays', v_pending_paydays);\n\n    -- =========================================\n    -- 🌟 停止マスの判定と自動処理（Babyマスの統合）\n    -- =========================================\n    IF v_new_pos IN (3, 16) THEN\n        v_cell_name := '寄付マス';\n        v_cell_msg := '寄付マスです。総収入の10%を支払い、向こう3ターンサイコロを2個振る権利を得るか選択してください（任意のアクションです）。';\n    \n    ELSIF v_new_pos = 9 THEN\n        -- Babyマスの自動計算処理\n        v_children_count := COALESCE((v_state->>'children_count')::int, 0);\n        v_per_child_expense := COALESCE((v_financials->>'per_child_expense')::int, 0);\n\n        IF v_children_count < 3 THEN\n            v_children_count := v_children_count + 1;\n            v_state := jsonb_set(v_state, '{children_count}', to_jsonb(v_children_count));\n\n            v_expenses := jsonb_set(v_expenses, '{child_expense}', to_jsonb(v_children_count * v_per_child_expense));\n            v_financials := jsonb_set(v_financials, '{expenses}', v_expenses);\n\n            v_total_expenses := \n                COALESCE((v_expenses->>'taxes')::int, 0) +\n                COALESCE((v_expenses->>'mortgage_payment')::int, 0) +\n                COALESCE((v_expenses->>'school_loan_payment')::int, 0) +\n                COALESCE((v_expenses->>'car_loan_payment')::int, 0) +\n                COALESCE((v_expenses->>'credit_card_payment')::int, 0) +\n                COALESCE((v_expenses->>'retail_payment')::int, 0) +\n                COALESCE((v_expenses->>'other_expenses')::int, 0) +\n                COALESCE((v_expenses->>'child_expense')::int, 0) +\n                COALESCE((v_expenses->>'bank_loan_payment')::int, 0);\n\n            v_financials := jsonb_set(v_financials, '{total_expenses}', to_jsonb(v_total_expenses));\n            v_total_income := COALESCE((v_financials->>'total_income')::int, 0);\n            v_financials := jsonb_set(v_financials, '{net_cash_flow}', to_jsonb(v_total_income - v_total_expenses));\n\n            v_state := jsonb_set(v_state, '{financials}', v_financials);\n            \n            -- ★ 計算フラグを立てる（先ほどセットした false を上書きして true にする）\n            v_flags := v_flags || jsonb_build_object('is_calculating', true);\n\n            v_cell_name := 'Babyマス';\n            v_cell_msg := '子供が生まれました！（現在の子供の数: ' || v_children_count || '人）養育費が追加されたため、財務諸表を再計算してください。';\n        ELSE\n            v_cell_name := 'Babyマス';\n            v_cell_msg := 'Babyマスに止まりましたが、子供の数はすでに最大（3人）です。費用は変動しません。';\n        END IF;\n\n    ELSIF v_new_pos = 20 THEN\n        v_cell_name := 'リストラマス';\n        v_cell_msg := 'リストラ（解雇）マスに止まりました。アクションを実行してください。';\n    ELSIF v_new_pos IN (1, 7, 14) THEN\n        v_cell_name := '娯楽マス';\n        v_cell_msg := '娯楽（Doodad）マスに止まりました。カードを引いてください。';\n    ELSIF v_new_pos IN (12, 22) THEN\n        v_cell_name := '市場マス';\n        v_cell_msg := '市場（Market）マスに止まりました。カードを引いてください。';\n    ELSIF v_new_pos IN (2, 4, 6, 8, 10, 13, 15, 17, 19, 21, 23) THEN\n        v_cell_name := '好機マス';\n        v_cell_msg := '好機（Deal）マスに止まりました。カードを引いてください。';\n    ELSIF v_new_pos IN (0, 5, 11, 18) THEN\n        v_cell_name := '入金マス';\n        v_cell_msg := '入金（Payday）マスに止まりました。';\n    END IF;\n\n    UPDATE participants \n    SET state = v_state || jsonb_build_object(\n        'position', v_new_pos,\n        'last_dice', v_dice,\n        'calculation_phase', CASE WHEN v_pending_paydays > 0 THEN 'payday' ELSE 'none' END,\n        'flags', v_flags\n    )\n    WHERE room_id = p_room_id AND user_id = p_user_id;\n\n    -- 1. サイコロの結果ログ\n    PERFORM public.fn_insert_game_log(\n        p_room_id,\n        v_user_name,\n        'サイコロ結果',\n        'サイコロを ' || p_dice_count || ' 個振り、【' || v_dice || '】を出しました。'\n    );\n\n    -- 2. 停止マスの判定とログ記録\n    IF v_cell_name IS NOT NULL THEN\n        PERFORM public.fn_insert_game_log(\n            p_room_id,\n            v_user_name,\n            v_cell_name,\n            v_cell_msg\n        );\n    END IF;\nEND;\n$function$\n"
  },
  {
    "function_name": "set_game_log_sequence",
    "function_definition": "CREATE OR REPLACE FUNCTION public.set_game_log_sequence()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\nBEGIN\n    SELECT COALESCE(MAX(sequence_num), 0) + 1 INTO NEW.sequence_num\n    FROM public.game_logs\n    WHERE room_id = NEW.room_id;\n    RETURN NEW;\nEND;\n$function$\n"
  },
  {
    "function_name": "start_game_with_professions",
    "function_definition": "CREATE OR REPLACE FUNCTION public.start_game_with_professions(p_room_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    r_part RECORD;\n    r_prof RECORD;\n    cur_prof CURSOR FOR SELECT id, name, starting_cash::varchar::int AS starting_cash, salary::varchar::int AS salary, expenses, initial_liabilities FROM professions ORDER BY RANDOM();\n    v_total_exp int;\n    v_net_cash_flow int;\n    v_per_child_expense int;\n    \n    v_deck_small jsonb;\n    v_deck_big jsonb;\n    v_deck_market jsonb;\n    v_deck_doodad jsonb;\nBEGIN\n    RAISE NOTICE '[DEBUG] start_game_with_professions START: room_id=%', p_room_id;\n\n    PERFORM id FROM rooms WHERE id = p_room_id FOR UPDATE;\n    PERFORM user_id FROM participants WHERE room_id = p_room_id FOR UPDATE;\n\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_small FROM cards WHERE deck_type = 'small_deal';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_big FROM cards WHERE deck_type = 'big_deal';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_market FROM cards WHERE deck_type = 'market';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_doodad FROM cards WHERE deck_type = 'doodad';\n\n    OPEN cur_prof;\n\n    FOR r_part IN SELECT user_id, state FROM participants WHERE room_id = p_room_id LOOP\n        FETCH cur_prof INTO r_prof;\n        \n        IF NOT FOUND THEN\n            CLOSE cur_prof;\n            OPEN cur_prof;\n            FETCH cur_prof INTO r_prof;\n        END IF;\n\n        v_per_child_expense := COALESCE(\n            (r_prof.expenses->>'per_child_expense')::int,\n            (r_prof.expenses->>'child_expense')::int, \n            0\n        );\n\n        -- ★修正: すべての支出項目を網羅\n        v_total_exp := \n            COALESCE((r_prof.expenses->>'taxes')::int, 0) + \n            COALESCE((r_prof.expenses->>'mortgage_payment')::int, 0) +\n            COALESCE((r_prof.expenses->>'school_loan_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'car_loan_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'credit_card_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'retail_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'other_expenses')::int, 0);\n\n        v_net_cash_flow := r_prof.salary - v_total_exp;\n\n        UPDATE participants \n        SET state = jsonb_build_object(\n            'name', COALESCE(r_part.state->>'name', 'Player'),\n            'role', COALESCE(r_part.state->>'role', 'general'),\n            'game_phase', 'rat_race',\n            'position', 0, \n            'last_dice', 0, \n            'calculation_phase', 'none', \n            'profession', r_prof.name, \n            'profession_id', r_prof.id,\n            'children_count', 0, \n            'flags', jsonb_build_object(\n                'has_rolled_dice', false,\n                'is_card_drawn', false,\n                'is_action_completed', false,\n                'is_calculating', false,\n                'is_negative_cash_flow', (v_net_cash_flow < 0),\n                'charity_turns_left', 0,\n                'downsized_turns_left', 0,\n                'pending_paydays', 0\n            ),\n            'financials', jsonb_build_object(\n                'cash', r_prof.starting_cash, \n                'salary', r_prof.salary,\n                'total_income', r_prof.salary,\n                'passive_income', 0,\n                'total_expenses', v_total_exp,\n                'net_cash_flow', v_net_cash_flow,\n                'per_child_expense', v_per_child_expense,\n                'expenses', COALESCE(r_prof.expenses, '{}'::jsonb) || jsonb_build_object(\n                    'bank_loan_payment', 0,\n                    'child_expense', 0\n                ),\n                'liabilities', COALESCE(r_prof.initial_liabilities, '{}'::jsonb) || jsonb_build_object(\n                    'bank_loan', 0\n                ), \n                'assets', jsonb_build_object('stocks', '{}'::jsonb, 'real_estate', '[]'::jsonb)\n            )\n        )\n        WHERE room_id = p_room_id AND user_id = r_part.user_id;\n    END LOOP;\n    \n    CLOSE cur_prof;\n\n    UPDATE rooms SET \n        game_state = jsonb_build_object(\n            'status', 'playing', \n            'decks', jsonb_build_object(\n                'small_deal', v_deck_small, \n                'big_deal', v_deck_big, \n                'market', v_deck_market, \n                'doodad', v_deck_doodad\n            ), \n            'current_card', null\n        ),\n        current_turn_user_id = (SELECT user_id FROM participants WHERE room_id = p_room_id ORDER BY RANDOM() LIMIT 1)\n    WHERE id = p_room_id;\n\n    RAISE NOTICE '[DEBUG] start_game_with_professions END: Initialize completes';\nEND;\n$function$\n"
  },
  {
    "function_name": "start_game_with_professions_v2",
    "function_definition": "CREATE OR REPLACE FUNCTION public.start_game_with_professions_v2(p_room_id text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\nAS $function$\nDECLARE\n    r_part RECORD;\n    r_prof RECORD;\n    cur_prof CURSOR FOR SELECT id, name, starting_cash::varchar::int AS starting_cash, salary::varchar::int AS salary, expenses, initial_liabilities FROM professions ORDER BY RANDOM();\n    v_total_exp int;\n    v_net_cash_flow int;\n    v_per_child_expense int;\n    \n    v_deck_small jsonb;\n    v_deck_big jsonb;\n    v_deck_market jsonb;\n    v_deck_doodad jsonb;\nBEGIN\n    -- 部屋と参加者のロック\n    PERFORM id FROM rooms WHERE id = p_room_id FOR UPDATE;\n    PERFORM user_id FROM participants WHERE room_id = p_room_id FOR UPDATE;\n\n    -- デッキのシャッフル\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_small FROM cards WHERE deck_type = 'small_deal';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_big FROM cards WHERE deck_type = 'big_deal';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_market FROM cards WHERE deck_type = 'market';\n    SELECT COALESCE(jsonb_agg(id ORDER BY random()), '[]'::jsonb) INTO v_deck_doodad FROM cards WHERE deck_type = 'doodad';\n\n    OPEN cur_prof;\n\n    FOR r_part IN SELECT user_id, state FROM participants WHERE room_id = p_room_id LOOP\n        FETCH cur_prof INTO r_prof;\n        \n        IF NOT FOUND THEN\n            CLOSE cur_prof;\n            OPEN cur_prof;\n            FETCH cur_prof INTO r_prof;\n        END IF;\n\n        v_per_child_expense := COALESCE(\n            (r_prof.expenses->>'per_child_expense')::int,\n            (r_prof.expenses->>'child_expense')::int, \n            0\n        );\n\n        v_total_exp := \n            COALESCE((r_prof.expenses->>'taxes')::int, 0) + \n            COALESCE((r_prof.expenses->>'mortgage_payment')::int, 0) +\n            COALESCE((r_prof.expenses->>'school_loan_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'car_loan_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'credit_card_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'retail_payment')::int, 0) + \n            COALESCE((r_prof.expenses->>'other_expenses')::int, 0);\n\n        v_net_cash_flow := r_prof.salary - v_total_exp;\n\n        UPDATE participants \n        SET state = jsonb_build_object(\n            'name', COALESCE(r_part.state->>'name', 'Player'),\n            'role', COALESCE(r_part.state->>'role', 'general'),\n            'game_phase', 'rat_race',\n            'position', 0, \n            'last_dice', 0, \n            'calculation_phase', 'none', \n            'profession', r_prof.name, \n            'profession_id', r_prof.id,\n            'children_count', 0, \n            'flags', jsonb_build_object(\n                'has_rolled_dice', false,\n                'is_card_drawn', false,\n                'is_action_completed', false,\n                'is_calculating', false,\n                'is_negative_cash_flow', (v_net_cash_flow < 0),\n                'charity_turns_left', 0,\n                'downsized_turns_left', 0,\n                'pending_paydays', 0\n            ),\n            'financials', jsonb_build_object(\n                'cash', r_prof.starting_cash, \n                'salary', r_prof.salary,\n                'total_income', r_prof.salary,\n                'passive_income', 0,\n                'total_expenses', v_total_exp,\n                'net_cash_flow', v_net_cash_flow,\n                'per_child_expense', v_per_child_expense,\n                'expenses', COALESCE(r_prof.expenses, '{}'::jsonb) || jsonb_build_object(\n                    'bank_loan_payment', 0,\n                    'child_expense', 0\n                ),\n                'liabilities', COALESCE(r_prof.initial_liabilities, '{}'::jsonb) || jsonb_build_object(\n                    'bank_loan', 0\n                ), \n                'assets', jsonb_build_object('stocks', '{}'::jsonb, 'real_estate', '[]'::jsonb)\n            )\n        )\n        WHERE room_id = p_room_id AND user_id = r_part.user_id;\n\n        -- 個別のプレイヤーの職業決定ログ\n        PERFORM public.fn_insert_game_log(\n            p_room_id, \n            COALESCE(r_part.state->>'name', 'Player'), \n            '職業決定', \n            '職業が「' || r_prof.name || '」に決定しました。（初期資金: $' || r_prof.starting_cash || '）'\n        );\n    END LOOP;\n    \n    CLOSE cur_prof;\n\n    UPDATE rooms SET \n        game_state = jsonb_build_object(\n            'status', 'playing', \n            'decks', jsonb_build_object(\n                'small_deal', v_deck_small, \n                'big_deal', v_deck_big, \n                'market', v_deck_market, \n                'doodad', v_deck_doodad\n            ), \n            'current_card', null\n        ),\n        current_turn_user_id = (SELECT user_id FROM participants WHERE room_id = p_room_id ORDER BY RANDOM() LIMIT 1)\n    WHERE id = p_room_id;\n\n    -- 全体へのゲーム開始システムログ\n    PERFORM public.fn_insert_game_log(\n        p_room_id, 'System', 'ゲーム開始', \n        'ゲームが開始されました。最初のプレイヤーの手番です。'\n    );\nEND;\n$function$\n"
  }
]
